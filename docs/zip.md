# Zip Files

```sh
# Get the tools
$ sudo apt-get install zip

# Make an archive
$ zip archive_name file1 file2 file3...

# Make a password protected archive:
$ zip -re out.zip infiles...
#> will prompt for the password twice
```

